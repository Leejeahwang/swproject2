# 🚀 빠른 시작 가이드

## MongoDB 설치 없이 바로 시작! 

이 프로젝트는 **lowdb**를 사용하여 별도의 데이터베이스 설치 없이 바로 실행할 수 있습니다!

## 1단계: 백엔드 서버 실행

```bash
# 이미 npm install은 완료되었습니다!

# 서버 시작
npm run dev
```

✅ 서버가 http://localhost:5000 에서 실행됩니다!
✅ 데이터는 `database/db.json` 파일에 자동으로 저장됩니다!

## 2단계: 프론트엔드 실행 (새 터미널)

```bash
cd client
npm start
```

✅ React 앱이 http://localhost:3000 에서 실행됩니다!

## 3단계: 사용해보기

1. http://localhost:3000 접속
2. 회원가입하기
3. 제품 등록하기
4. 대여 요청하기

## 데이터 확인하기

`database/db.json` 파일을 열어보면 저장된 모든 데이터를 확인할 수 있습니다!

```json
{
  "users": [...],
  "products": [...],
  "rentals": [...],
  "reviews": [...],
  "chats": [...]
}
```

## 주요 기능

✨ **회원가입/로그인** - JWT 인증
✨ **제품 등록** - 이미지 업로드 포함
✨ **제품 검색** - 카테고리, 지역, 가격 필터
✨ **대여 시스템** - 요청, 승인, 완료 플로우
✨ **신뢰도 시스템** - 🎯 -100~100점 점수 시스템
✨ **리뷰 작성** - 양방향 평가
✨ **실시간 채팅** - Socket.io

## 문제 해결

### 포트가 이미 사용 중이라는 오류
- 다른 프로그램이 5000번 또는 3000번 포트를 사용 중입니다
- 해당 프로그램을 종료하거나 `.env` 파일에서 포트 변경

### 이미지 업로드가 안 됨
- `uploads/` 폴더가 생성되어 있는지 확인
- 없다면: `mkdir uploads`

### 데이터가 초기화되지 않음
- `database/db.json` 파일을 삭제하면 새로 생성됩니다

## 나중에 MongoDB로 전환하기

개발이 진행되면 MongoDB로 전환할 수 있습니다. 
자세한 내용은 `README.md`의 "lowdb에서 MongoDB로 전환하기" 섹션을 참고하세요.

---

즐거운 개발 되세요! 🎉

